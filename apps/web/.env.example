# Public env vars consumed by apps/web. Only NEXT_PUBLIC_* values reach the
# browser. Copy to `.env.local` for development.

# Address of the NestJS backend the browser calls directly. Defaults to
# http://localhost:3000 when unset (see next.config.js).
NEXT_PUBLIC_API_URL=http://localhost:3000
